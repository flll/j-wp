version: '3.8'
services:
  mariadb:
    container_name: db_${SITE_NAME}
    image: 'jbergstroem/mariadb-alpine'
    volumes:
      - ~/j.d/db.d/${SITE_NAME}:/var/lib/mysql:cached
      - /etc/localtime:/etc/localtime
    environment:
      MYSQL_ROOT_PASSWORD: ${ROOTPASSWD}
      MYSQL_DATABASE: wp
      MYSQL_USER: wp_user
      MYSQL_PASSWORD: ${DBPASSWD}
      MYSQL_CHARSET: utf8
      MYSQL_COLLATION: utf8_general_ci
      LANG: C.UTF-8
    restart: unless-stopped

  wordpress:
    container_name: wp_${SITE_NAME}
    depends_on:
      - mariadb
    image: docker.io/wordpress:php7.4-fpm-alpine
    networks: 
    - web-net-c
    volumes:
      - ~/j.d/nginx.d/${SITE_NAME}:/var/www/html
      - ~/j.d/site/conf.d/${SITE_NAME}.php.ini:/usr/local/etc/php/conf.d/custom.ini:ro
      - ~/j.d/site/conf.d/${SITE_NAME}.php-fpm.ini:/usr/local/etc/php-fpm.d/php-fpm.ini
      - ~/j.d/log:/log:cached
      - /etc/localtime:/etc/localtime
    environment:
      WORDPRESS_DB_HOST: db_${SITE_NAME}:3306
      WORDPRESS_DB_PASSWORD: ${DBPASSWD}
      WORDPRESS_DB_NAME: wp
      WORDPRESS_DB_USER: wp_user
      WORDPRESS_CONFIG_EXTRA: |
        define('WPLANG',           'ja');
        define('DB_CHARSET',       'utf8');
        define('DB_COLLATE',       'utf8_general_ci');
        define('FORCE_SSL_ADMIN',   true);
      LANG: C.UTF-8
    restart: unless-stopped

networks:
  web-net-c:
    external:
      name: web-net